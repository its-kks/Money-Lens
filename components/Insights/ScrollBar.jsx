import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function ScrollBar({itemsList, setItem}) {
  return (
    <View>
      <Text>ScrollBar</Text>
    </View>
  )
}

const styles = StyleSheet.create({})